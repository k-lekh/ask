ask_examples = read(ideas/*/*.ask)
decompose read(ideas/*/*.ask) `
  Rule how to split the content into array
``
  Rule what to do with each piece
``
  Rule how to combine all back into a single string
`