// extracts number of pages of a content and removes it from the original file
// the size of the page is dependent on model
// usually one page is the max token limit / 2, so basically
// page = limit / 2.
// this function should use no ai